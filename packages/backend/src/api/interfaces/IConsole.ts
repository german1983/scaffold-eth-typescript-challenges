export enum shapes {
    SHAPE1,
    SHAPE2,
}

export interface IConsole {
    shape: shapes;
    backColor: string;
    middleColor: string;
    frontColor: string;
    buttonColor: string;
    lineColor: string;
}
